# 具身智能仿真工程师 · 学习路线

> **目标岗位**：具身智能仿真工程师（非算法岗）  
> **主线顺序**：基础语言 → 数学基础 → 机器人理论 → 仿真工具 → 环境接口 → 通信框架 → 数据管线 → Sim2Real  
> **能力分级**：`必须精通` / `看懂原理即可` / `浅尝了解`

**背景优势**：Blender / 3ds Max、Unity / UE、前端 —— 资产与场景管线要尽早用上。  
**硬件提示**：Isaac Sim 需要较好的 NVIDIA GPU；暂无独显时先走 MuJoCo + 资产/URDF，Isaac 可延后或上云。

---

## 总览时序（按此执行）

| 顺序 | 阶段 | 重点 |
|------|------|------|
| 0 | 前置 | Python 工程基础 + NumPy |
| 1 | 并行支线 | Blender/UE → URDF/USD 资产小流程 |
| 2 | 阶段 A | 机器人数学（变换、四元数） |
| 3 | 阶段 A | 运动学 / 动力学 / PID（看懂+调参） |
| 4 | 阶段 B | MuJoCo + MJCF/URDF |
| 5 | 阶段 B | 相机模型 + OpenCV（轻量，赶在 Isaac 传感器前） |
| 6 | 阶段 B | USD + Isaac Sim 入门 |
| 7 | 阶段 B | Isaac Lab / Gym 环境接口 + 域随机/并行/采集 |
| 8 | 阶段 C | ROS2（Python）与仿真互通 |
| 9 | 阶段 D | 数据集管线 |
| 10 | 阶段 E | Docker / 远程可视化 |
| 11 | 阶段 E | Sim2Real 概念与联调 |
| 12 | 阶段 F | 算法接口认知 + **最小训练闭环**（不深调） |

---

## 前置：Python + NumPy

**定位**：`必须精通`（不硬卡「7 天」，以能独立写小工具为准）

### Python 工程基础

**语法与结构**

- 变量、数字 / 字符串、`list` / `tuple` / `dict` / `set`
- 条件、`for` / `while`、推导式
- 函数：参数、返回值、默认参数；分清可变对象引用（`b = a` 与 `copy`）
- 类与对象：`__init__`、方法、简单继承（仿真里 Entity / Env 都是类）
- 模块：`import`、`from ... import`、自己拆文件

**工程习惯**

- 虚拟环境：`venv` / `conda`，`pip install`、`requirements.txt`
- 会读报错栈（`TypeError` / `ValueError` / `IndexError` 等）
- 会读简单开源仓库的 `README` 并跑通示例
- 文件读写：文本、简单 JSON；路径用 `pathlib` 更好

**完成标准**：能把「输入 → 计算函数 → 打印/保存」拆开写，而不是全堆在一起。

---

### NumPy（仿真数学的日常工具）

**数组基本功（必须熟练）**

- 创建：`np.array`、`np.zeros`、`np.ones`、`np.eye`、`np.arange`、`np.linspace`
- 属性：`shape`、`dtype`、`ndim`、`size`
- 变形：`reshape`、`ravel` / `flatten`、`T`（转置）、`transpose`
- 拼接与拆分：`np.concatenate`、`np.stack`、`np.vstack` / `np.hstack`、`np.split`
- 索引：切片、布尔索引、花式索引；分清视图 vs 拷贝

**运算（必须熟练）**

- 逐元素运算与**广播（broadcasting）**规则
- 矩阵乘法：`@` / `np.matmul`（和 `*` 逐元素相乘的区别一定要清楚）
- `np.dot`、向量模长、归一化
- 常用聚合：`sum` / `mean` / `max` / `min` / `argmax`
- 线性代数入口：`np.linalg.inv`、`np.linalg.norm`、`np.linalg.solve`（会用即可）

**和机器人/仿真直接相关的练习**

- 用 `(4,)` / `(4, 4)` 表示齐次坐标点与变换矩阵
- 批量点变换：`(N, 3)` 点云 × 旋转 / 齐次矩阵
- 随机数：`np.random`（后面域随机会用到）

**完成标准**

- 不查文档也能熟练用：`shape`、`reshape`、`T`、`@`、广播
- 能手写：旋转矩阵作用到点、两个 `4×4` 变换相乘、批量点坐标转换

**不必深挖**：NumPy C 扩展原理、极少用的冷门函数

---

## 并行支线：3D 资产 → 仿真模型（发挥长板）

**定位**：`必须精通`（差异化竞争力，与主线并行，不必等数学全学完）

### 建模与场景

- Blender / 3ds Max / UE：做一个**简单机械臂**或**桌面抓取场景**（零件不必很精美）
- 控制面数；分好 link / joint 对应的物体层级
- 材质先简单（颜色 / 粗糙度），为后面域随机留接口

### 导出与规范（仿真能否跑起来全看这里）

- **单位**：米制；缩放别在仿真里才发现差 100 倍
- **原点与轴向**：右手法则；关节旋转轴方向要对
- **碰撞体**：尽量用简单几何（盒 / 柱 / 球），别直接用高模当 collision
- **命名**：link、joint 命名清晰、可脚本批量改
- 导出目标：**URDF**（MuJoCo/ROS 常用）、**USD**（Isaac 核心）

### 转换与验证

- URDF ↔ USD 工具链跑通一次
- 导入仿真后检查：重力下落、关节能否转、有无穿模

**学习产出**：一份可导入 MuJoCo 或 Isaac 的简易机器人/场景资产（含碰撞与关节）。

---

## 阶段 A：数学与机器人基础

### A1. 线性代数 + 机器人运动学数学

**定位**：`必须精通`（仿真底层根基）

**向量与矩阵**

- 点 vs 向量；齐次坐标 `(x, y, z, 1)`
- 旋转矩阵性质（正交、`R.T @ R = I`、行列式为 1）
- 欧拉角：常见约定（注意万向锁）
- 四元数：表示旋转、归一化、与旋转矩阵互转（会用库或手写核心公式）
- 平移 + 旋转 → **齐次变换矩阵 T（4×4）**
- SE(3)：变换复合 `T_ab @ T_bc = T_ac`；求逆 `T_ba`

**坐标系（高频实战）**

- 世界系 / 基座系 / 末端系 / 相机系
- URDF 里 `origin xyz rpy` 在说什么
- 左右手系、相机 OpenCV 坐标系 vs 机器人坐标系差异（有概念即可）

**NumPy 实操清单**

- [ ] 欧拉角 → 旋转矩阵 → 作用到点
- [ ] 四元数 ↔ 旋转矩阵
- [ ] 两个连杆变换相乘，求末端位姿
- [ ] 相机外参：把点从世界系变到相机系

**不必深挖**：高等代数证明、泛函分析、李群李代数推导（知名词即可）

**学习产出**：坐标变换脚本 —— 基座 ↔ 相机 ↔ 机械臂坐标系互转，并打印验证。

**里程碑作品 ①（可提前做）**：NumPy 多坐标系变换小工具

---

### A2. 机器人基础理论

**定位**：`看懂原理即可`

**正 / 逆运动学**

- FK：关节角 → 末端位姿（会算或会调库）
- IK：末端位姿 → 关节角（知道有解析/数值法；会调 IK 求解器）
- DH 参数或 URDF 链式结构（能看懂描述即可）

**雅可比（概念级）**

- 关节速度 ↔ 末端速度关系
- 奇异位形是什么、仿真里为什么会「卡住/发疯」

**动力学与接触（直觉）**

- 质量、惯性、质心
- 摩擦、碰撞、穿透与仿真步长 `timestep` 的关系
- 力矩控制 vs 位置控制 vs 速度控制（仿真 API 里常见）

**PID**

- P / I / D 各干什么；调参直觉（超调、震荡、稳态误差）
- 在仿真里给关节一个 PID，让末端跟目标

**边界**：能看文档、能调参；不必从零手写完整动力学引擎。

---

## 阶段 B：仿真工具栈（核心竞争力 · 主线）

### B1. MuJoCo（入门首选）

**定位**：`必须精通`

**模型（MJCF / URDF）**

- `body` / `joint` / `geom` / `actuator` / `sensor` 分别是什么
- 关节类型：hinge、slide、free 等
- 碰撞组、摩擦系数、阻尼
- 从 URDF 导入或手写一个最小 MJCF

**Python API**

- 加载模型、`mj_step` 仿真循环
- 读状态：关节位置 `qpos`、速度 `qvel`
- 写控制：`ctrl`、位置/速度/力矩执行器
- 读传感器、接触信息（入门级）

**进阶一点**

- 重置环境、设置初始姿态
- 简单域随机：物体位置/颜色/摩擦随机
- 相机渲染、保存 RGB / depth（若版本支持）

**里程碑作品 ②**：MuJoCo 机械臂场景 + 关节控制到目标位姿 + 简单相机出图

---

### B2. 相机与视觉基础（提前学，服务传感器）

**定位**：`看懂原理即可`（**放在 Isaac 传感器之前或同步**）

**相机模型**

- 针孔模型；焦距、主点
- **内参 K**、**外参**（R、t 或 T_cam_world）
- 畸变有个印象即可
- 深度图：每个像素是什么；单位、无效值

**OpenCV 基础**

- `imread` / `imwrite`、颜色空间 BGR/RGB
- 裁剪、缩放、画框
- 像素坐标 ↔ 归一化平面（结合内参）
- 深度图 → 点云的最小实现（循环或向量化）

**跳过**：检测/分割网络训练、复杂多视图重建

---

### B3. USD 与 3D 资产工作流

**定位**：`必须精通`（贴合 UE/Unity）

**USD 基础**

- Stage、Prim、Attribute
- Xform、Mesh、材质绑定
- 关节 / Articulation 在 USD 里如何表达（结合 Isaac 文档）
- 层（Layer）、引用（Reference）—— 方便场景复用

**工作流**

- URDF → USD；USD 再进 Isaac
- 场景分层：机器人 / 桌子 / 物体 / 灯光分开
- 批量替换物体、随机摆放（为域随机打基础）

---

### B4. Isaac Sim（主力工具）

**定位**：`必须精通`

**入门**

- 安装与启动（本机 GPU 或云）
- Python / Script Editor 跑官方示例
- 导入机器人与场景（USD）

**传感器**

- RGB、Depth、IMU、Lidar（会挂、会读数据）
- 相机位姿与内外参在 Isaac 里怎么设
- 同步：仿真一步 ↔ 取一帧

**核心能力**

- Domain Randomization：光照、材质、位姿、物理参数
- 并行环境思路（多环境同时跑）
- 录数据：图像、关节、位姿写到磁盘

---

### B5. Isaac Lab / Gym 环境接口（必补）

**定位**：`必须精通`（对接算法的关键接口）

**Gymnasium 思维（先建立）**

- `observation_space` / `action_space`
- `reset()` → 初始观测
- `step(action)` → `obs, reward, terminated, truncated, info`
- 一局（episode）何时结束

**Isaac Lab（或同类框架）**

- 任务配置：机器人、场景、成功条件
- 并行环境：`num_envs`
- 改观测（加关节、加图像特征入口）、改动作（关节目标 / 增量）
- 接一个最简 RL 或模仿学习示例跑通（会改配置即可）

**里程碑作品 ③**：Isaac/Lab 域随机 + 导出合成数据，**或** 最小训练闭环跑通并截图/录屏

---

## 阶段 C：机器人通信框架

### C1. ROS2（Humble / Jazzy 等，Python 为主）

**定位**：`看懂原理即可`（工程打通即可）  
**建议时机**：Isaac / Lab 入门**之后**（不要卡在 Isaac 前面）

**核心概念**

- 节点 Node、话题 Topic、服务 Service、动作 Action（知道用途）
- 消息 Msg、包 Package、工作空间
- `ros2 topic list/echo`、`ros2 node list` 等命令行

**Python 实操**

- 写 Publisher：发关节角、位姿、图像
- 写 Subscriber：收速度/力矩指令
- 常用类型：`sensor_msgs/Image`、`JointState`、`Pose`/`PoseStamped`、`Twist`

**与仿真结合**

- MuJoCo / Isaac 一步仿真 ↔ 发布传感器；订阅控制指令写回仿真
- URDF 在 ROS 里的角色（`robot_state_publisher` 有印象即可）

**不必学**：ROS2 内核源码、C++ 高性能组件、复杂 Launch 玄学（会改现成即可）

---

## 阶段 D：数据管线

### D1. 数据集工程

**定位**：`看懂原理即可` + 脚本能写

**格式与组织**

- 文件夹结构：`images/`、`labels/`、`meta.json` 等
- 常见格式印象：YOLO、COCO；RL 用的 trajectory（obs/action/reward）
- 命名与索引对齐（图号 ↔ 标注 ↔ 关节状态）

**工程点**

- 多进程 / 多环境采集时如何避免写文件冲突
- 异步写盘、缓冲队列（概念 + 简单实现）
- 清洗：坏图、全黑深度、NaN、长度不一致的轨迹

**学习产出**：仿真自动采 N 条样本并按约定格式落盘的脚本。

---

## 阶段 E：工程化加分

### E1. 容器与云端仿真

**定位**：`浅尝了解` → 用到再加深（可发挥前端 / 云）

- Dockerfile：打包依赖与启动命令
- 卷挂载：代码 / 数据集进出容器
- 远程桌面或 Web 流：Pixel Streaming / Omniverse 流媒体等（知原理、会跟文档搭）
- 多机并行：概念上知道「调度多个仿真进程」

---

### E2. Sim2Real 基础

**定位**：`看懂原理即可`

- 为何 sim 好 real：动力学、传感器噪声、延迟、材质
- 域随机要随机哪些参数（视觉 + 物理）
- 参数辨识 / 标定：相机外参、关节零位
- 真机链路：急停、限速、仿真指令如何安全下发（安全意识）

---

## 阶段 F：具身算法认知（边界清晰）

**定位**：`浅尝了解` + **能跑通最小闭环**

**概念（弄懂 I/O 与数据接口）**

- 强化学习：策略 π、奖励、探索；和仿真 `step` 的关系
- 模仿学习 / 行为克隆：示范数据长什么样
- Diffusion Policy、VLA、RT-1/RT-2：输入是图/语言/状态，输出是动作（不必会训）
- 世界模型：名词级

**最小动手（建议做一次）**

- 在自己环境或官方小环境上跑通 **1 个** 官方训练脚本
- 会改：环境名、`num_envs`、观测里加一个字段、保存 checkpoint
- **不要求**：刷榜、改网络结构、精调大模型

**职责边界**：搭环境、供数据、对齐接口；策略效果由算法同学主责。

---

## 作品集里程碑（求职用）

| # | 作品 | 对应阶段 | 建议展示内容 |
|---|------|----------|--------------|
| 1 | NumPy 多坐标系变换小工具 | 阶段 A1 | 代码 + 几组数值验证 |
| 2 | MuJoCo 机械臂 + 控制 + 相机 | 阶段 B1 | 视频/动图 + MJCF/URDF |
| 3 | Isaac/Lab：域随机 + 数据或最小训练 | 阶段 B4–B5 | 管线说明 + 样本或训练曲线 |

---

## 明确不做（避开算法岗冗余）

- 高等代数 / 泛函等纯理论证明
- 从零手写物理求解器
- ROS2 C++ 内核级开发
- 复杂 CV 训练、大模型精调、刷 SOTA

---

## 一句话原则

> **资产与场景是长板，仿真环境与数据接口是饭碗，算法只求能对接。**
